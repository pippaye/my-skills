# img2drawio

Codex skill for converting structured diagram images into faithful, editable draw.io files.

See `SKILL.md` for the runtime workflow. Upstream draw.io references can be refreshed with:

```bash
./update.sh
```
